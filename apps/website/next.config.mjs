/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['UplyftHer.s3.eu-central-1.amazonaws.com','dnfmf20x7jj78.cloudfront.net'], // Add the S3 bucket domain here
    },
};

export default nextConfig;
